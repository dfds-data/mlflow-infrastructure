name: Build Docker Image

on:
  pull_request:
    paths:
      - 'mlflow-image/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        env:
          DOCKER_BUILDKIT: "1"
        run: docker build ./mlflow-image
